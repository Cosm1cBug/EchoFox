# EchoFox Command Catalog

> Auto-generated by `scripts/generate-command-docs.js` — do not edit by hand.
> Re-run with `npm run docs:commands` after adding or modifying commands.

**55** commands across **10** categories.

## Table of contents

- [admin](#admin) — 4
- [convert](#convert) — 3
- [download](#download) — 7
- [entertainment](#entertainment) — 1
- [general](#general) — 13
- [group](#group) — 5
- [main](#main) — 1
- [misc](#misc) — 7
- [tools](#tools) — 12
- [user](#user) — 2

## admin

| Command            | Aliases                            | Description                                                                              |
| ------------------ | ---------------------------------- | ---------------------------------------------------------------------------------------- |
| **`.ai-admin`**    | `.aiadmin`                         | (admin) AI ops: stats / chats / limit / enable / disable _🔒 admin · ⏱️ 2s cooldown_     |
| **`.healthcheck`** | `.health`, `.diag`, `.diagnostics` | (admin) Run a full self-diagnostic and reply with a summary _🔒 admin · ⏱️ 10s cooldown_ |
| **`.purge`**       | `.clear`, `.cleanup`               | Revoke the bot's recently-sent messages in this chat. _🔒 admin · ⏱️ 5s cooldown_        |
| **`.serverinfo`**  | `.sinfo`, `.host`                  | (admin) Show host + process vitals _🔒 admin_                                            |

## convert

| Command        | Aliases                    | Description                                                                                       |
| -------------- | -------------------------- | ------------------------------------------------------------------------------------------------- |
| **`.sticker`** | `.s`, `.stiker`            | Convert quoted image/short video to a WhatsApp sticker _⏱️ 5s cooldown_                           |
| **`.toimg`**   | `.toimage`, `.sticker2img` | Convert a quoted sticker into a PNG image _⏱️ 5s cooldown_                                        |
| **`.tts`**     | `.text2speech`, `.speak`   | Convert quoted text to speech. Optional 2-letter language code translates first. _⏱️ 5s cooldown_ |

## download

| Command          | Aliases                              | Description                                                                          |
| ---------------- | ------------------------------------ | ------------------------------------------------------------------------------------ |
| **`.apk`**       | `.apkdl`, `.aptoide`                 | Search Aptoide for an Android app and get download info _⏱️ 10s cooldown_            |
| **`.dwnlod`**    | `.fetchmedia`, `.grab`, `.savemedia` | Save a quoted media message back to the chat _⏱️ 5s cooldown_                        |
| **`.mediafire`** | `.mfdl`, `.mf`                       | Download a file from a MediaFire share URL _⏱️ 20s cooldown_                         |
| **`.pinterest`** | `.pinterestdl`, `.pin`               | Search Pinterest and send back image results _⏱️ 8s cooldown_                        |
| **`.song`**      | `.songdl`, `.yt`                     | Search YouTube for a song and reply with the audio (MP3) _⏱️ 15s cooldown_           |
| **`.spotify`**   | `.spotifydl`, `.spdl`                | Download a Spotify track as MP3 (via YouTube Music) _⏱️ 20s cooldown_                |
| **`.ytdl`**      | `.ytd`, `.youtubedl`                 | Download a YouTube video or audio (gated by config.features.ytdl). _⏱️ 30s cooldown_ |

## entertainment

| Command     | Aliases                      | Description                                                                      |
| ----------- | ---------------------------- | -------------------------------------------------------------------------------- |
| **`.omdb`** | `.movie`, `.series`, `.imdb` | Look up a movie or series on OMDb _🔑 needs `apis.omdb.apiKey` · ⏱️ 4s cooldown_ |

## general

| Command               | Aliases                             | Description                                                                                        |
| --------------------- | ----------------------------------- | -------------------------------------------------------------------------------------------------- |
| **`.ai`**             | `.llm`, `.gpt`                      | Per-chat AI control: status / on / off / clear / persona / provider / model _⏱️ 2s cooldown_       |
| **`.alienvault`**     | `.pulse`                            | _(undocumented)_                                                                                   |
| **`.ask`**            | `.askme`, `.aiq`                    | One-shot AI query (no memory, no opt-in needed). _⏱️ 10s cooldown_                                 |
| **`.explain`**        | `.eli5`, `.wat`                     | AI explanation of a replied message or arbitrary text. _⏱️ 10s cooldown_                           |
| **`.github`**         | `.gh`                               | _(undocumented)_                                                                                   |
| **`.imagine`**        | `.img`, `.gen`, `.dalle`            | Generate an image from a text prompt (OpenAI Images). _⏱️ 60s cooldown_                            |
| **`.rss`**            | `.feed`, `.feeds`                   | _(undocumented)_                                                                                   |
| **`.summarize`**      | `.sum`, `.tldr`, `.recap`           | AI summary of recent messages (or replied message). _⏱️ 30s cooldown_                              |
| **`.thehackersnews`** | `.thn`, `.hackernews`, `.cybernews` | _(undocumented)_                                                                                   |
| **`.translate`**      | `.tr`, `.trt`                       | Translate a quoted text message to the given language _⏱️ 3s cooldown_                             |
| **`.virustotal`**     | `.vt`                               | Lookup IP / domain / URL / hash on VirusTotal _🔑 needs `apis.virustotal.apiKey` · ⏱️ 5s cooldown_ |
| **`.vtwatch`**        | `.vt-watch`, `.vtw`                 | _(undocumented)_                                                                                   |
| **`.wiki`**           | `.wikipedia`                        | Search Wikipedia and return article excerpt _⏱️ 4s cooldown_                                       |

## group

| Command         | Aliases                           | Description                                                                      |
| --------------- | --------------------------------- | -------------------------------------------------------------------------------- |
| **`.antilink`** | `.nolink`, `.linkblock`           | Configure antilink protection for this group. _👥 group · ⏱️ 2s cooldown_        |
| **`.approve`**  | `.accept`                         | Approve a pending join request in this group _👥 group_                          |
| **`.link`**     | `.grouplink`, `.invite`           | Get the invite link for this group _👥 group · ⏱️ 10s cooldown_                  |
| **`.warn`**     | `.warning`, `.warnings`, `.warns` | Group-admin warn system with auto-kick at threshold. _👥 group · ⏱️ 2s cooldown_ |
| **`.welcome`**  | `.greet`, `.goodbye`              | Configure this group's welcome/goodbye messages. _👥 group · ⏱️ 3s cooldown_     |

## main

| Command     | Aliases     | Description                       |
| ----------- | ----------- | --------------------------------- |
| **`.menu`** | `.listmenu` | Shows all the available features. |

## misc

| Command             | Aliases               | Description                                                            |
| ------------------- | --------------------- | ---------------------------------------------------------------------- |
| **`.antiviewonce`** | `.vv`, `.avv`         | Re-send a quoted view-once media as a regular message _⏱️ 5s cooldown_ |
| **`.ctxmsg`**       | `.ctest`              | Print the parsed message context (debug helper) _⏱️ 3s cooldown_       |
| **`.ctxi`**         | `.inspect`, `.whoami` | Print the parsed message context (debug helper) _⏱️ 3s cooldown_       |
| **`.exec`**         | `.>`                  | (admin) Evaluate JS expression OR run shell command _🔒 admin_         |
| **`.ping`**         | `.p`                  | Check ping and bot responsiveness. _⏱️ 2s cooldown_                    |
| **`.quote`**        | `.q`, `.inspire`      | Fetch a random inspirational quote _⏱️ 5s cooldown_                    |
| **`.sendstory`**    | `.savestory`, `.sst`  | Save a quoted Status (story) image/video _⏱️ 5s cooldown_              |

## tools

| Command         | Aliases                           | Description                                                          |
| --------------- | --------------------------------- | -------------------------------------------------------------------- |
| **`.base64`**   | `.b64`                            | Base64 encode / decode. _⏱️ 2s cooldown_                             |
| **`.convert`**  | `.cv`, `.fx`, `.currency`         | Currency conversion (fiat + crypto). _⏱️ 5s cooldown_                |
| **`.define`**   | `.def`, `.dict`                   | English dictionary lookup. _⏱️ 5s cooldown_                          |
| **`.hash`**     | `.digest`                         | Hash text with md5/sha1/sha256/sha384/sha512. _⏱️ 2s cooldown_       |
| **`.poll`**     | `.vote`                           | Create a native WhatsApp poll. _⏱️ 5s cooldown_                      |
| **`.qr`**       | `.qrcode`, `.qrgen`               | Generate a QR-code image from text or a URL. _⏱️ 5s cooldown_        |
| **`.remindme`** | `.remind`, `.reminder`            | Schedule a reminder ("⏰ in 10 minutes …"). _⏱️ 2s cooldown_         |
| **`.shorten`**  | `.short`, `.tinyurl`              | Shorten a URL via is.gd. _⏱️ 5s cooldown_                            |
| **`.ssweb`**    | `.ssw`, `.webshot`, `.screenshot` | Capture a screenshot of a webpage _⏱️ 8s cooldown_                   |
| **`.timezone`** | `.tz`, `.time`, `.worldclock`     | Current time in a timezone or city. _⏱️ 5s cooldown_                 |
| **`.uuid`**     | `.guid`, `.id`                    | Generate UUIDs / random short IDs / hex strings. _⏱️ 1s cooldown_    |
| **`.weather`**  | `.w`, `.forecast`                 | Current conditions and 3-day forecast for any city. _⏱️ 5s cooldown_ |

## user

| Command        | Aliases         | Description                                                     |
| -------------- | --------------- | --------------------------------------------------------------- |
| **`.afk`**     | `.away`         | Mark yourself as away with an optional reason. _⏱️ 2s cooldown_ |
| **`.profile`** | `.me`, `.whois` | Show your profile (or quoted user's profile) _⏱️ 5s cooldown_   |

---

## Legend

- 🔒 **admin** — only users in `config.admins` can run this command
- 👥 **group** — only works inside group chats
- 🔑 **needs** — auto-disabled at startup if listed config paths are empty
- ⏱️ **cooldown** — per-user delay between consecutive uses
